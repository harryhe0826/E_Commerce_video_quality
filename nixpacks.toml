# Nixpacks plan for Railway deployment
[phases.setup]
nixPkgs = ["python39", "python39Packages.pip", "ffmpeg", "gcc", "bash"]

[phases.install]
cmds = ["cd backend && python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["chmod +x start.sh"]

[start]
cmd = "./start.sh"

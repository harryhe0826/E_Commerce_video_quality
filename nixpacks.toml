# Nixpacks plan for Railway deployment
[phases.setup]
nixPkgs = ["python39", "ffmpeg", "gcc", "bash"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["chmod +x start.sh"]

[start]
cmd = "./start.sh"

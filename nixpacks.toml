# Nixpacks plan for Railway deployment
# Nixpacks will auto-detect Python from requirements.txt in root
[phases.setup]
nixPkgs = ["ffmpeg", "gcc", "glib", "stdenv.cc.cc.lib"]
nixLibs = ["zlib", "libGL"]

[phases.build]
cmds = ["chmod +x start.sh"]

[start]
cmd = "./start.sh"
